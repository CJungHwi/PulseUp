# 로컬 개발용 — 이 파일을 복사해 `.env`로 쓰세요 (`packages/electron-app/.env`).
# Electron 메인이 기동 시 로드합니다. 이미 셸/cross-env로 설정된 값은 덮어쓰지 않습니다.

# EMOM 메인 재생만 단축( DB 세션 duration 그대로 )
HIIT_EMOM_TEST_INTERVAL_SEC=5
# HIIT_EMOM_TEST_DURATION_SCALE=0.0166667
