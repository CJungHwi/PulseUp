/**
 * 타이머 플레이 HTML 조립은 `circuits/shared` 및 서킷별 `timer-layout-intro` 에서 관리.
 * 이 파일은 기존 import 경로(`./workout-play-timer-layout.js`) 호환용 얇은 재export.
 */
export { createWorkoutPlayTimerHtml } from '../circuits/shared/timer-play-layout-compose.js'
