# Workout Execution Format

`tools/workout-validation.ts` 실행 결과를 `workout_exercises_logic.md` 스타일로 풀어쓴 문서입니다.

## 1. Stress

### 1-1. 운동 6개 / 3라운드

운동:
`L1, L2, L3, R1, R2, R3`

패널:
- Round 1: `60초`, 휴식 `20초`
- Round 2: `40초`, 휴식 `15초`
- Round 3: `20초`, 휴식 `10초`, 물보충 `60초`

실제 생성 결과:

```text
전반전:
L1: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
L2: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
L3: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R3: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R2: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R1: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)

후반전 없음
```

판정:
- 운동 6개만 있으므로 후반전 없음
- 마지막 물보충은 생성되지 않음

### 1-2. 운동 12개 / 전후반 분리

운동:
`L1, L2, L3, R1, R2, R3, L4, L5, L6, R4, R5, R6`

패널:
- Round 1: `60초`, 휴식 `20초`
- Round 2: `40초`, 휴식 `15초`
- Round 3: `20초`, 휴식 `10초`, 물보충 `60초`

실제 생성 결과:

```text
전반전:
L1: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
L2: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
L3: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R3: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R2: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R1: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→물보충 60초

후반전:
L4: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
L5: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
L6: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R6: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R5: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)→10초(휴식)
R4: 60초(운동)→20초(휴식)→40초(운동)→15초(휴식)→20초(운동)
```

판정:
- 전반 마지막 `R1` 뒤에만 물보충 `1회`
- 후반 마지막 뒤에는 물보충 없음

## 2. Loop

### 2-1. 운동 6개 / 마지막 water 생략

운동:
`L1, L2, L3, R1, R2, R3`

패널:
- Round 1: `60초`, 휴식 `20초`
- Round 2: `60초`, 휴식 `20초`
- Round 3: `60초`, 휴식 `20초`, 물보충 `60초`

실제 생성 결과:

```text
전반전:
Round 1: L1(60초)→휴식20초→L2(60초)→휴식20초→L3(60초)→휴식20초→R3(60초)→휴식20초→R2(60초)→휴식20초→R1(60초)→휴식20초
Round 2: L1(60초)→휴식20초→L2(60초)→휴식20초→L3(60초)→휴식20초→R3(60초)→휴식20초→R2(60초)→휴식20초→R1(60초)→휴식20초
Round 3: L1(60초)→휴식20초→L2(60초)→휴식20초→L3(60초)→휴식20초→R3(60초)→휴식20초→R2(60초)→휴식20초→R1(60초)

후반전 없음
```

판정:
- 운동 6개라 후반전이 없어 마지막 물보충 없음

### 2-2. 운동 12개 / 전후반 water

운동:
`L1, L2, L3, R1, R2, R3, L4, L5, L6, R4, R5, R6`

패널:
- Round 1: `60초`, 휴식 `20초`
- Round 2: `60초`, 휴식 `20초`
- Round 3: `60초`, 휴식 `20초`, 물보충 `60초`

실제 생성 결과:

```text
전반전:
Round 1: L1(60초)→휴식20초→L2(60초)→휴식20초→L3(60초)→휴식20초→R3(60초)→휴식20초→R2(60초)→휴식20초→R1(60초)→휴식20초
Round 2: L1(60초)→휴식20초→L2(60초)→휴식20초→L3(60초)→휴식20초→R3(60초)→휴식20초→R2(60초)→휴식20초→R1(60초)→휴식20초
Round 3: L1(60초)→휴식20초→L2(60초)→휴식20초→L3(60초)→휴식20초→R3(60초)→휴식20초→R2(60초)→휴식20초→R1(60초)→물보충60초

후반전:
Round 4: L4(60초)→휴식20초→L5(60초)→휴식20초→L6(60초)→휴식20초→R6(60초)→휴식20초→R5(60초)→휴식20초→R4(60초)→휴식20초
Round 5: L4(60초)→휴식20초→L5(60초)→휴식20초→L6(60초)→휴식20초→R6(60초)→휴식20초→R5(60초)→휴식20초→R4(60초)→휴식20초
Round 6: L4(60초)→휴식20초→L5(60초)→휴식20초→L6(60초)→휴식20초→R6(60초)→휴식20초→R5(60초)→휴식20초→R4(60초)
```

판정:
- 전반 마지막 `Round 3` 끝에만 물보충 1회 생성

## 3. AMRAP

### 3-1. 1패널 / 운동 6개

운동:
`L1, L2, L3, R1, R2, R3`

패널:
- Round 1: `10분`, 휴식 `1분`, 물보충 `0`

실제 생성 결과:

```text
전반전 (Round 1):
L1(10분/12회)→L2(10분/12회)→L3(10분/12회)→R3(10분/12회)→R2(10분/12회)→R1(10분/12회)

후반전 없음
```

판정:
- 그룹이 1개뿐이라 휴식/물보충 없음

### 3-2. 2패널 / 운동 12개

운동:
`L1, L2, L3, R1, R2, R3, L4, L5, L6, R4, R5, R6`

패널:
- 전반: `10분`, 휴식 `1분`, 물보충 `1분`
- 후반: `5분`, 휴식 `0분`, 물보충 `0분`

실제 생성 결과:

```text
전반전 (Round 1):
L1(10분/12회)→L2(10분/12회)→L3(10분/12회)→R3(10분/12회)→R2(10분/12회)→R1(10분/12회)→물보충 1분→휴식 1분

후반전 (Round 2):
L4(5분/12회)→L5(5분/12회)→L6(5분/12회)→R6(5분/12회)→R5(5분/12회)→R4(5분/12회)
```

판정:
- 전후반 사이에서만 물보충/휴식 삽입

## 4. EMOM

### 4-1. 운동 6개 / 후반전 없음

운동:
`L1, L2, L3, R1, R2, R3`

패널:
- Round 1: `3분`, 휴식 `0`
- Round 2: `2분`, 휴식 `0`
- Round 3: `1분`, 휴식 `1분`, 물보충 `0`

실제 생성 결과:

```text
전반전 : L1(3분/10회)→L2(3분/10회)→L3(3분/10회)→R3(3분/10회)→R2(3분/10회)→R1(3분/10회)(1Round 끝)
→L1(2분/10회)→L2(2분/10회)→L3(2분/10회)→R3(2분/10회)→R2(2분/10회)→R1(2분/10회)(2Round 끝)
→L1(1분/10회)→L2(1분/10회)→L3(1분/10회)→R3(1분/10회)→R2(1분/10회)→R1(1분/10회)(3Round 끝)

후반전 없음
```

판정:
- 운동 6개라 후반전이 없어서 마지막 휴식/물보충 생성 안 됨

### 4-2. 운동 12개 / Round 3 water

운동:
`L1, L2, L3, R1, R2, R3, L4, L5, L6, R4, R5, R6`

패널:
- Round 1: `3분`, 휴식 `0`
- Round 2: `2분`, 휴식 `0`
- Round 3: `1분`, 휴식 `0`, 물보충 `1분`

실제 생성 결과:

```text
전반전 : L1(3분/10회)→L2(3분/10회)→L3(3분/10회)→R3(3분/10회)→R2(3분/10회)→R1(3분/10회)(1Round 끝)
→L1(2분/10회)→L2(2분/10회)→L3(2분/10회)→R3(2분/10회)→R2(2분/10회)→R1(2분/10회)(2Round 끝)
→L1(1분/10회)→L2(1분/10회)→L3(1분/10회)→R3(1분/10회)→R2(1분/10회)→R1(1분/10회)→물보충 1분 (3Round 끝)

후반전 : L4(3분/10회)→L5(3분/10회)→L6(3분/10회)→R6(3분/10회)→R5(3분/10회)→R4(3분/10회)(4Round 끝)
→L4(2분/10회)→L5(2분/10회)→L6(2분/10회)→R6(2분/10회)→R5(2분/10회)→R4(2분/10회)(5Round 끝)
→L4(1분/10회)→L5(1분/10회)→L6(1분/10회)→R6(1분/10회)→R5(1분/10회)→R4(1분/10회)(6Round 끝)
```

판정:
- 전반 마지막 `Round 3` 끝에만 물보충 1회 생성

### 4-3. 운동 12개 / Round 3 rest

운동:
`L1, L2, L3, R1, R2, R3, L4, L5, L6, R4, R5, R6`

패널:
- Round 1: `3분`, 휴식 `0`
- Round 2: `2분`, 휴식 `0`
- Round 3: `1분`, 휴식 `1분`, 물보충 `0`

실제 생성 결과:

```text
전반전 : L1(3분/10회)→L2(3분/10회)→L3(3분/10회)→R3(3분/10회)→R2(3분/10회)→R1(3분/10회)(1Round 끝)
→L1(2분/10회)→L2(2분/10회)→L3(2분/10회)→R3(2분/10회)→R2(2분/10회)→R1(2분/10회)(2Round 끝)
→L1(1분/10회)→L2(1분/10회)→L3(1분/10회)→R3(1분/10회)→R2(1분/10회)→R1(1분/10회)→휴식 1분 (3Round 끝)

후반전 : L4(3분/10회)→L5(3분/10회)→L6(3분/10회)→R6(3분/10회)→R5(3분/10회)→R4(3분/10회)(4Round 끝)
→L4(2분/10회)→L5(2분/10회)→L6(2분/10회)→R6(2분/10회)→R5(2분/10회)→R4(2분/10회)(5Round 끝)
→L4(1분/10회)→L5(1분/10회)→L6(1분/10회)→R6(1분/10회)→R5(1분/10회)→R4(1분/10회)(6Round 끝)
```

판정:
- 전반 마지막 `Round 3` 끝에만 휴식 1회 생성

## 5. Preload

### 5-1. Stress / AMRAP

```text
Stress:
- timeline preload 1건 생성 확인

AMRAP:
- timeline preload 1건 생성 확인
```

### 5-2. Loop / EMOM

```text
Loop:
- timeline preload 생성 확인
- 다음 그룹(L4~R4) preload 생성 확인
- 물보충 중 다음 그룹 preload 생성 확인
- Cool Down preload 생성 확인

EMOM:
- timeline preload 생성 확인
- 다음 그룹(L4~R4) preload 생성 확인
- 물보충 중 다음 그룹 preload 생성 확인
- Cool Down preload 생성 확인
```
